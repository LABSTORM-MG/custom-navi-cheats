% image, view, open, feh

# displays the given image
feh <image>

$ image: ls | grep -E '\.jpg$|\.png$|\.gif$'


% color picker

# starts Gpick
gpick


% update oh my posh, omp

$ curl -s https://ohmyposh.dev/install.sh | sudo bash -s